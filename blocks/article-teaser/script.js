const toolbar=document.querySelector('[data-stage-toolbar]'),frame=document.querySelector('[data-stage-frame]'),variantsTarget=document.querySelector('[data-variants]');async function loadJson(u){const r=await fetch(u,{cache:'no-store'});if(!r.ok)throw new Error(String(r.status));return r.json()}function empty(){frame.innerHTML='<div class="stage-empty"><strong>Noch kein Beispiel vorhanden.</strong><p>Sobald HTML-Beispiele im Ordner examples/ liegen und das Manifest erzeugt wurde, werden sie hier automatisch geladen.</p></div>'}async function variants(){try{const d=await loadJson('./examples/variants.json');if(!d.variants?.length)return;variantsTarget.innerHTML='';for(const i of d.variants){const li=document.createElement('li');li.textContent=i.label;variantsTarget.append(li)}}catch{}}async function examples(){try{const d=await loadJson('./examples/manifest.json');if(!d.examples?.length){empty();return}toolbar.innerHTML='';d.examples.forEach((e,n)=>{const b=document.createElement('button');b.type='button';b.textContent=e.label;b.setAttribute('aria-pressed',n===0?'true':'false');b.onclick=()=>{toolbar.querySelectorAll('button').forEach(x=>x.setAttribute('aria-pressed','false'));b.setAttribute('aria-pressed','true');frame.innerHTML='';const f=document.createElement('iframe');f.src='./examples/'+e.file;f.title=e.label;f.loading='lazy';frame.append(f)};toolbar.append(b)});toolbar.querySelector('button')?.click()}catch{empty()}}variants();examples();